<?xml version="1.0" encoding="UTF-8"?>
<!--
  Hadoop hdfs-site.xml — local single-node HDFS (Nagy, plan Task 1).
  This file is for reference; the apache/hadoop:3.3.6 Docker image reads
  configuration from environment variables declared in hadoop.env (preferred).
-->
<configuration>
  <property>
    <name>dfs.replication</name>
    <value>1</value>
    <description>Single datanode for local development — raise in production.</description>
  </property>
  <property>
    <name>dfs.namenode.rpc-bind-host</name>
    <value>0.0.0.0</value>
    <description>Bind RPC to all interfaces so containers on scf-net can reach it.</description>
  </property>
  <property>
    <name>dfs.permissions.enabled</name>
    <value>false</value>
    <description>Disable POSIX permissions for local dev convenience.</description>
  </property>
  <property>
    <name>dfs.namenode.name.dir</name>
    <value>/tmp/hadoop-root/dfs/name</value>
  </property>
  <property>
    <name>dfs.datanode.data.dir</name>
    <value>/tmp/hadoop-root/dfs/data</value>
  </property>
</configuration>
